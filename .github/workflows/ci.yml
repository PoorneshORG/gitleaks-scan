name: SecurityPipeline

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']
  workflow_dispatch:

jobs:
  secret-scan:
    name: Gitleaks-proxy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Run Secret Scan with Composite Action
        uses: PoorneshORG/gitleaks-scan@ghcr-image-action
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

