name: 🔐 Gitleaks Secret Scan + DefectDojo Upload

on:
  pull_request:
    types:
      - opened
      - synchronize
      - labeled
      - ready_for_review
      - reopened

  workflow_dispatch:

jobs:
  secret-scan:
    name: 🔐 Gitleaks + DefectDojo Upload
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write   # ✅ allows commenting on PRs

    # ✅ Skip the job if PR is closed and merged
    if: ${{ github.event.pull_request.state == 'open' }}

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Secret Scan & Upload
        id: scan
        uses: PoorneshORG/gitleaks-scan@pr-diff-action
        continue-on-error: true
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          dojo-url: ${{ secrets.DEFECTDOJO_URL }}
          dojo-api-key: ${{ secrets.DEFECTDOJO_API_KEY }}
          product-name: DevSecOps
          report-file: gitleaks-report.json
          repo-name: ${{ github.repository }}
          branch-name: ${{ github.head_ref || github.ref_name }}


      # - name: 💬 Comment on PR if secrets are detected
      #   if: github.event_name == 'pull_request'
      #   uses: actions/github-script@v7
      #   with:
      #     script: |
      #       const fs = require('fs');
      #       const path = process.env.GITHUB_WORKSPACE + '/gitleaks-report.json';

      #       try {
      #         const findings = JSON.parse(fs.readFileSync(path, 'utf8'));
      #         if (findings.length > 0) {
      #           await github.rest.issues.createComment({
      #             issue_number: context.issue.number,
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             body: 'hello'
      #           });
      #         }
      #       } catch (e) {
      #         core.warning("Could not read " + path + " → " + e.message);
      #       }
